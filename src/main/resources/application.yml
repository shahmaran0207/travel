spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/Travel?serverTimeZone=UTC
    username: root
    password: Since2018@
  thymeleaf:
    prefix: classpath:/templates/
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: 8083
  mvc:
    view:
      suffix: .html

  application:
    name: travel

  logging:
    level:
      org:
        springframework:
          security: DEBUG
        hibernate:
          type:
            descriptor.sql: trace